# 战机大乱斗 —— 发布到网上 / 拿到公开链接 保姆级教程

> 目标：把游戏放到互联网上，得到一个**发给别人点开就能玩**的公开链接。
> 你现在本地 `http://localhost:8181` 只能自己访问；上传到下面任意平台后，任何人、任何设备点链接都能玩（手机竖屏、电脑横屏自动适配）。

---

## 0. 你要上传的东西

我已经帮你把游戏打包好了，直接用这个 zip：

- **`E:\小游戏\战机大乱斗-网页版.zip`**（约 5.8 MB，里面有 `index.html`、`assets/`（所有图片）、`manifest.webmanifest`、`sw.js`、`icons/`）

下面任选**一种**方式即可。

---

## 方式一：itch.io（推荐，最简单，适合游戏）

1. 打开 https://itch.io ，注册一个免费账号（用邮箱即可）。
2. 登录后点右上角 **Upload new project**。
3. **Type** 选 **HTML**。
4. 名称填 **战机大乱斗**。
5. 在 **Upload files** 处，把 `战机大乱斗-网页版.zip` 传上去（选 This file will be played in the browser，默认即是）。
6. 建议勾选 **Mobile friendly**（让手机端更顺滑）。
7. 点 **Save & publish** 发布。
8. 发布后你会得到类似 `https://你的用户名.itch.io/战机大乱斗` 的**公开链接**，发给别人，对方点开就能直接进游戏。

> 说明：itch.io 有时会套一层自己的页面，玩家要点一次 Play 才进游戏（这是正常现象）；加个收藏也能直接玩。

---

## 方式二：GitHub Pages（免费、长期、稳定）

1. 打开 https://github.com ，注册免费账号。
2. 点右上角 **+** → **New repository**，填仓库名（如 `fighter-game`），选 **Public**，点 **Create**。
3. 点 **uploading an existing file**，把 zip 解压后的文件（`index.html`、`assets`、`manifest.webmanifest`、`sw.js`、`icons`）拖进去，点 **Commit**。
4. 进入仓库 **Settings → Pages**，把 **Source** 选为 `Deploy from a branch`，分支选 `main`，根目录 `/`，点 **Save**。
5. 等 1~2 分钟，地址变成 `https://你的用户名.github.io/仓库名/`，这就是公开链接。

---

## 方式三：Netlify（拖拽即部署）

1. 打开 https://app.netlify.com ，用邮箱/账号登录。
2. 点 **Add new site → Deploy manually**。
3. 把解压后的整个文件夹（或直接用 zip）**拖进去**。
4. 几秒后得到 `https://随机名字.netlify.app` 的链接，可在 **Site settings → Change site name** 改成好记的名字。

---

## 注意事项

- 链接是**公开的**：谁拿到都能玩。
- 游戏进度（积分、已购武器/皮肤）保存在**每个人自己的浏览器**里——玩家 A 买了东西，玩家 B 看不到；换设备/换浏览器也不通用（这是浏览器游戏的正常机制）。
- 首次打开会**直接进入游戏**（已跳过教程和菜单）；想回主菜单/商店，点游戏右上角「停止」。
- 上面的链接平台都提供 **HTTPS**，离线缓存（Service Worker）也能正常工作。
